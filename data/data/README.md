# Data folder

Place the raw dataset here:

```text
data/raw/OnlineNewsPopularity.csv
```

Generated processed files will be saved to:

```text
data/processed/
```

The raw dataset is ignored by `.gitignore` to avoid committing data files unless the course instructions allow it.
