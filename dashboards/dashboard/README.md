# Dashboard

This folder contains a starter Streamlit dashboard.

Run it from the repository root with:

```bash
streamlit run dashboard/app.py
```

The dashboard currently expects generated results in:

```text
data/processed/classification_results.csv
```

Complete the dashboard after the final Task A and Task B outputs are saved.
